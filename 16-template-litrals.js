// Template Literals are special type of strings which make creating complex strings easier
// create strings using using template literals
const person = {
  name: 'JI_DEvv',
  age: 15,
};

// Template literals with multi-line and strings interpolation
const greeting = `Hello, ${person.name}!
your age is ${person.age}.`;

console.log(greeting);
